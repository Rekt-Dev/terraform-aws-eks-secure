# S3 backend configuration is in main.tf
# This file is a placeholder to document the backend setup requirements.
#
# Before running terraform init, the S3 bucket and DynamoDB table must exist.
# Create them using the s3-backend/ configuration first:
#
#   cd ../../s3-backend
#   terraform init && terraform apply
#
# Then return here and run:
#   terraform init
#   terraform plan
#   terraform apply
